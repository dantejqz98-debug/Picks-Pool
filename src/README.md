# Source Modules

The current app is still kept in `index.html` so the live site behavior stays unchanged. This folder is included so the project is ready to grow into normal Vite modules later without changing the Firebase project or existing data.
